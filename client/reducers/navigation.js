//Spread = ready to accept a new spread Journal = add reflection component

const navigation = (state = 'spread', action) => {
  switch (action.type) {
    default: return state
  }
}

export default navigation